import 'package:flutter/material.dart';

class AppColors {
  // Main Theme Colors
  static const Color primary = Color(
    0xFF10C46B,
  ); // A bright green for primary actions
  static const Color accent = Color(
    0xFF3B82F6,
  ); // A blue for secondary actions/info
  static const Color background = Color(0xFFF5F5F5); // Light grey background

  // Card Backgrounds (Subtle color for the dashboard card backgrounds)
  static const Color cardBgHotel = Color(
    0xFFE8F5E9,
  ); // Very light green for Hotel Dashboard
  static const Color cardBgNgo = Color(
    0xFFE3F2FD,
  ); // Very light blue for NGO Dashboard

  // Text and Status Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color success = Color(0xFF4CAF50);
  static const Color info = Color(0xFF2196F3);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFF44336);
}
